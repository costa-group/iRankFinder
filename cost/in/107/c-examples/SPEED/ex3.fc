{
  vars: [A,B,C],
  pvars: [A',B',C'],
  initnode: evalfstart,
  nodes: {
    evalfbb1in: {
      cfr_properties: [
        // RFs Properties
        [-A + B >= 0],
        [A -B > 0],
        [B -C >= 0],
        [-B + C > 0],
      ],
      inv_polyhedra: [-A + C >= 0, -1 + B -C >= 0, A >= 0],
    },
    evalfbb2in: {
      cfr_properties: [
        // RFs Properties
        [-A + B >= 0],
        [A -B > 0],
        [B -C >= 0],
        [-B + C > 0],
      ],
      inv_polyhedra: [-A + C >= 0, -1 -A + B >= 0, B -C >= 0, A >= 0],
    },
    evalfbb3in: {
      cfr_properties: [
        // RFs Properties
        [-A + B >= 0],
        [A -B > 0],
        [B -C >= 0],
        [-B + C > 0],
      ],
      inv_polyhedra: [-A + C >= 0, -1 + B -C >= 0, A >= 0],
    },
    evalfbb4in: {
      cfr_properties: [
        // RFs Properties
        [-A + B >= 0],
        [A -B > 0],
      ],
      inv_polyhedra: [A >= 0, -A + C >= 0, -1 -A + B >= 0],
    },
    evalfbb5in: {
      cfr_properties: [
        // RFs Properties
        [-A + B >= 0],
        [A -B > 0],
      ],
      inv_polyhedra: [A >= 0],
    },
    evalfbb6in: {
      cfr_properties: [
        // RFs Properties
        [-A + B >= 0],
        [A -B > 0],
      ],
      inv_polyhedra: [-1 -A + B >= 0, A >= 0],
    },
    evalfentryin: {
      inv_polyhedra: [],
    },
    evalfreturnin: {
      inv_polyhedra: [A >= 0],
    },
    evalfstart: {
      inv_polyhedra: [],
    },
    evalfstop: {
      inv_polyhedra: [],
    },
  },
  transitions: [
    {
	source: evalfstart,
	target: evalfentryin,
	name: t0,
	constraints: [A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfentryin,
	target: evalfbb5in,
	name: t1,
	constraints: [-A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb3in,
	target: evalfbb1in,
	name: t10,
	constraints: [D -1 >= 0, A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb3in,
	target: evalfbb4in,
	name: t11,
	constraints: [A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb1in,
	target: evalfbb2in,
	name: t12,
	constraints: [A -A' = 0, B -B' = 0, C + 1 -C' = 0]
    },
    {
	source: evalfbb4in,
	target: evalfbb5in,
	name: t13,
	constraints: [C + 1 -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfreturnin,
	target: evalfstop,
	name: t14,
	constraints: []
    },
    {
	source: evalfbb5in,
	target: evalfreturnin,
	name: t2,
	constraints: [A -B >= 0, A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb5in,
	target: evalfbb6in,
	name: t3,
	constraints: [B -A -1 >= 0, A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb6in,
	target: evalfbb2in,
	name: t4,
	constraints: [-1 -D >= 0, A -A' = 0, B -B' = 0, A -C' = 0]
    },
    {
	source: evalfbb6in,
	target: evalfbb2in,
	name: t5,
	constraints: [D -1 >= 0, A -A' = 0, B -B' = 0, A -C' = 0]
    },
    {
	source: evalfbb6in,
	target: evalfreturnin,
	name: t6,
	constraints: [A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb2in,
	target: evalfbb4in,
	name: t7,
	constraints: [C -B >= 0, A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb2in,
	target: evalfbb3in,
	name: t8,
	constraints: [B -C -1 >= 0, A -A' = 0, B -B' = 0, C -C' = 0]
    },
    {
	source: evalfbb3in,
	target: evalfbb1in,
	name: t9,
	constraints: [-1 -D >= 0, A -A' = 0, B -B' = 0, C -C' = 0]
    }
  ]
}
