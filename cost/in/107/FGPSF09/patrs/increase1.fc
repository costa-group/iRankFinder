{
  vars: [A,B],
  pvars: [A',B'],
  initnode: start,
  nodes: {
    f: {
      cfr_properties: [
        // RFs Properties
        [A -B >= 0],
        [-A + B > 0],
      ],
      inv_polyhedra: [],
    },
    start: {
      inv_polyhedra: [],
    },
  },
  transitions: [
    {
	source: f,
	target: f,
	name: t0,
	constraints: [A -B -1 >= 0, A -A' = 0, B + 1 -B' = 0]
    },
    {
	source: start,
	target: f,
	name: t1,
	constraints: [A -A' = 0, B -B' = 0]
    }
  ]
}
