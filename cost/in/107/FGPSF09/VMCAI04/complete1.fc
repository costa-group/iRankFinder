{
  vars: [A,B],
  pvars: [A',B'],
  initnode: start,
  nodes: {
    eval: {
      cfr_properties: [
        // RFs Properties
        [A -B >= 0],
        [-A + B > 0],
      ],
      inv_polyhedra: [],
    },
    start: {
      inv_polyhedra: [],
    },
  },
  transitions: [
    {
	source: eval,
	target: eval,
	name: t0,
	constraints: [A -B -1 >= 0, C >= 0, D -1 >= 0, A -C -A' = 0, B + D -B' = 0]
    },
    {
	source: start,
	target: eval,
	name: t1,
	constraints: [A -A' = 0, B -B' = 0]
    }
  ]
}
