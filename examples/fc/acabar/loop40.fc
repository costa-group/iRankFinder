{
  vars: [x,y,z],
  pvars: [x',y',z'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x >= 0, x + y >= 0, x + y + z - x' == 0, z + y' + 1 == 0, z - z' == 0]
    }
  ]
}
