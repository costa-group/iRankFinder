{
  vars: [x,y],
  pvars: [x',y'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x - 1 >= 0,  - x + y - 1 >= 0, 2 * x - x' == 0, y - y' + 1 == 0]
    }
  ]
}
