{
  vars: [x,y],
  pvars: [x',y'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [4 * x - 5 * y - 1 >= 0, 2 * x + 4 * y - x' == 0, 4 * x - y' == 0]
    }
  ]
}
