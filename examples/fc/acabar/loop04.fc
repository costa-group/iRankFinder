{
  vars: [x,y,n],
  pvars: [x',y',n'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [n - 201 >= 0,  - y + 8 >= 0,  - x + n - 1 >= 0, x + y - x' == 0,  - x' + 199 >= 0, y - y' == 0, n - n' == 0]
    },
    {
	source: n,
	target: n,
	name: t1,
	constraints: [n - 201 >= 0,  - y + 8 >= 0, x - n >= 0, x - x' == 0, y - y' == 0, n - n' == 0]
    }
  ]
}
