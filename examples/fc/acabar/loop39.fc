{
  vars: [x,y,z],
  pvars: [x',y',z'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x - 1 >= 0,  - x + y - 1 >= 0,  - 2 * x + x' - 1 >= 0, z - y' == 0, z - z' == 0]
    }
  ]
}
