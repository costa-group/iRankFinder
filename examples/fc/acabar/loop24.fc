{
  vars: [x,y,n],
  pvars: [x',y',n'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x - 1 >= 0,  - x + n - 1 >= 0, x - y + x' + 5 == 0, 2 * y - y' == 0, n - n' == 0]
    }
  ]
}
