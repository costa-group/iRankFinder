{
  vars: [x,y,z],
  pvars: [x',y',z'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x - 1 >= 0,  - x + z >= 0, 2 * x + y - x' == 0, y - y' + 1 == 0, z - z' == 0]
    }
  ]
}
