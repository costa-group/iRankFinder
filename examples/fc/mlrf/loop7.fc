{
  vars: [x,y],
  pvars: [x',y'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x + 10 >= 0,  - x + 10 >= 0,  - y + 10 >= 0, x + y - x' == 0, y - y' - 1 == 0]
    }
  ]
}
