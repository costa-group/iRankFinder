{
  vars: [x,y,z],
  pvars: [x',y',z'],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x >= 0,  - y + 10 >= 0,  - z + 1 >= 0, z >= 0, x + y + z - x' - 10 == 0, y - z - y' == 0, z + z' - 1 == 0]
    }
  ]
}
