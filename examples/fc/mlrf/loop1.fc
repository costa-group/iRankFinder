{
  vars: [x,y,z],
  pvars: [xp,yp,zp],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x + z >= 0, x + y - xp == 0, y + z - yp == 0, z - zp - 1 == 0]
    }
  ]
}
