{
  vars: [x,y],
  pvars: [xp,yp],
  initnode: n,
  transitions: [
    {
	source: n,
	target: n,
	name: t0,
	constraints: [x >= 0, y >= 0, x - xp - 1 == 0]
    },
    {
	source: n,
	target: n,
	name: t1,
	constraints: [x >= 0, y >= 0, x - xp == 0, y - yp - 1 == 0]
    }
  ]
}
